# fix 019 — Q-7 press 죽은 토큰 RCA (★처방 보류·N=2 재발 트리거 예약)

> **이건 처방 원장이 아니라 *보류 결정* 원장이다.** RCA는 완료, 처방은 measure-first로 미적용. 다음 런에서 *재발*하면 트리거 발동. 사후 합리화 차단 위해 "왜 안 고쳤나"를 고치기 전에 박는다.

## 메타
- **회차**: 019
- **트리거**: `results/20260623-0322-weather-claude.md`(Q-7 line 125·죽은 토큰 WEAK) + `-compare.md`(VW-4 line 20·48·codex 정상작동)
- **베이스 코퍼스**: `7c28c13` (불변 — 본 회차는 코퍼스 미수정)
- **시술 커밋**: **없음(보류)**
- **검증 런**: N/A (재발 감시만)
- **상태**: **보류(measure-first)** — 코퍼스 동결·재발 시 처방

## 증상
claude 15차: `AppDuration.press`·`.transition` 토큰을 *정의만* 하고 위젯 인용 0(grep)·press 효과 미구현(InkWell 기본) → **죽은 토큰**(비치명 🟡 WEAK·TIER-Q 상). **진짜 결함은 죽은 변수가 아니라 그것이 드러내는 "시안 눌림 효과(`active:scale-98`) 미구현"=디자인 충실도 흠**이다(죽은 변수는 미완성의 화석).

## RCA (적대 3렌즈 검증 — 본세션 `wf` 없이 Agent 3렌즈)

### 골격 = 견고 (렌즈 B: HOLDS) — 코퍼스 2중 공백 확정
- **공백 ①(긍정 규율 부재)**: "시안 인터랙션 상태(`active:`/`animate-`/press)가 있으면 위젯에 효과를 *구현하라*"는 규율이 코퍼스에 **확정 부재**. 현 규율(architecture-ui §7 `final.md:103`·houserules §5 `:258`)은 전부 "생 `Duration(...)` 박지 마라(있으면 토큰 경유)"는 **부정 방향**뿐 — press 효과가 *아예 없으면* 잡을 리터럴이 없어 공허 통과. design-architect triage 정형목록(`design-architect.md:38`)도 색/크기/이미지 **3트랙뿐 인터랙션 트랙 없음**.
- **공백 ②(죽은 토큰 블로킹 게이트 부재)**: "foundation 토큰을 정의했으면 인용하라(정의-미인용=죽은코드)"를 막는 *블로킹* 게이트 0건. 죽은코드는 cleancode *스멜*(`discipline-cleancode/references/final.md:2302`·비-블로킹)·reviewer는 죽은 *메서드*(`discipline-reviewer.md:53`)/죽은 *error채널*(`:60`·State 전용)만.
- **★강화 증거**: `scripts/extract_design.dart:334`가 추출 단계에서 `hover:`/`active:` 접두를 **기계적으로 제거** → 인터랙션 상태가 *데이터에서부터 폐기*되어 하류(명세·코더)에 앵커조차 없음. **규율 부재 이전에 데이터가 없다.**

### 인과 = 과장이었음 (렌즈 A: QUALIFIED — 1차 분석 교정)
1차 RCA "묶음2(`06a30ff`)가 14차 생리터럴→15차 죽은토큰으로 *전위*"는 **N=1 과장**. 교정 근거 3:
- **13차 선례**: 죽은 `AppDuration.transition` 토큰이 묶음2 *이전*(13차 `20260622-0323-weather-claude.md:128`)에 이미 발생 → "묶음2가 처음 만들었다"는 거짓.
- **엔진 비결정**: press 효과 구현이 12차(O `20260621-0203:61`)→13차→14차(O `20260622-1636:63`)→15차(X) 회차마다 출렁임(묶음2 무관).
- **codex 정상작동(★결정적)**: codex는 *같은* 묶음2 코퍼스로 `AppDuration.pressFeedback` 3곳 구현+인용(`-compare.md:20·48`) → **코퍼스 공백이 아니라 엔진 자유변수**.

→ 정직한 표현: *"묶음2가 `press` 토큰명을 coder 로드 경로(houserules §5)에 도입한 것은 견고(렌즈 A 대체경로 FAILED), 같은 런에서 claude가 효과를 빼며 토큰이 죽은 것은 관찰(인과 미확정)"*. 확률 귀속 **~75% 코퍼스 공백 / ~15% cleancode 비-블로킹 / ~10% N=2 엔진변동** — 적대 리뷰가 사전 가설 확증.

## 왜 보류인가 (렌즈 C + 3렌즈 수렴)
| 보류 근거 | 증거 |
|---|---|
| 비치명 WEAK·N=1 | 같은 15차 치명 2건(FID-L1·FC-2)이 픽스처 FAIL 실제 원인 — 처방 예산 우선순위 역전 |
| codex 정상작동 | 코퍼스는 올바르게 유도됨·claude만 자발 회피 = 핀할 자유변수 정체 모호(FC-2처럼 자발 회복 가능성 상존) |
| "인용" 진술 이미 존재 | `architecture-ui §7:103`이 *이미* "토큰에 의미값을 두고 **인용**한다" → 한 구절 추가는 **췌언**+동형 함정([[feedback-018]] ★렌즈B 교훈: 지식만 고치고 길목 안 고치면 무효) |
| 과적합 위험 | `active:`/`scale-98` 어휘를 규율에 박으면 범용 원칙 위반·"형상은 시안 소관" 직교 경계(impl-flutter §9·architecture-ui §8) 침범 |
| 큰 공사 | 효과를 *구현하게* 하려면 extract_design 인터랙션 보존 + architect triage 인터랙션 트랙 + impl-flutter §9 확장 = 파이프라인 3~4곳 |
| 측정 빈곤 | 죽은 토큰 자동 게이트 없음 → 처방해도 다음 런 또 육안 |

## 재발 트리거 (발동 조건 + 처방 입력 지도)
**조건**: 다음 라이브런(16차+)에서 claude(또는 codex)가 **press/인터랙션 미구현 또는 연출 토큰 死(정의-미인용)을 재발**하면 → N=2 성립·보류 해제·아래에서 선택 처방.

- **소(小) 처방** [죽은 변수 청소만·핵심 못 고침]: `discipline-reviewer.md §5`에 "foundation 토큰 정의-미인용 = 죽은코드·important" 감사 신호 1줄(죽은 *메서드* 패턴과 평행·인터랙션 어휘 미언급·범용). **양판**(claude `agents/` ∥ codex `dddart-discipline-reviewer/SKILL.md`). 한계: 효과를 아예 안 만들면 토큰도 없어 못 잡음.
- **대(大) 처방** [눌림 효과를 *구현하게*·근본]: ① `extract_design.dart`가 `active:`/`hover:` 접두를 버리지 말고 인터랙션 트랙으로 **보존**(단일출처 `tools/`) → ② `design-architect.md` triage에 **인터랙션 트랙 신설**(색/크기/이미지 옆) → ③ `implementation-flutter §9`(또는 신설 절)에 "시안 인터랙션 상태는 위젯에 구현" 규율. 비용 큼·과구속 위험 동반 검토.

## 회차 요약 (16차 후 — 재발 감시)
- **16차 실측(엔진 역전)**: codex `AppDuration.interaction` 죽은 토큰(`grep AppDuration.*` 인용 0)·press 미구현(InkWell 기본·15차 `pressFeedback` 인용에서 퇴행) / **claude는 press 구현**(`forecast_tile_widget.dart:52 AnimatedScale(scale:_pressed?0.98:1, duration:AppDuration.press)`·죽은 토큰 해소·15차 claude 死에서 회복).
- **N=2 성립**(실패모드 "press 死/미구현" — 15차 claude·16차 codex) — **단 같은 코퍼스 `1fc7946`로 claude 구현·codex 미구현 = RCA "엔진 비결정 자유변수" 강화**(코퍼스 단독 원인 아님이 더 확실). 15·16차 합쳐 한 엔진이 회복하면 다른 엔진이 퇴행 = 출렁임.
- **보류 유지 권고**: 대처방(인터랙션 구현 파이프라인 3~4곳·비용 큼·과구속)은 N=2에도 비권고. **소처방**(reviewer §5 "foundation 토큰 정의-미인용=죽은코드 important" 1줄·범용·저비용)은 **피드백 단계 사용자 결정 대기**. ⚠️ N=2지만 엔진 비결정이라 처방 효과 불확실(측정 빈곤 지속·죽은 토큰 자동 게이트 없음).
- **★사용자 보류 확정(2026-06-23)**: Q-6 처방(feedback-021) 직후 "Q-7도 같은 절차로 처방 가능?" 질의 → 분석: **소처방은 Q-6과 동일 절차·회귀 안전**이나 ① **표적 빗나감**(화석=죽은 토큰만 잡고 진짜 결함 press 미구현은 못 잡음 — 효과를 안 만들면 토큰도 없음·codex 16차가 그 경우) ② **측정 빈곤**(죽은 토큰 자동 게이트 0) ③ **N=2가 엔진 역전**이라 코퍼스 단독 원인 반증. 대처방은 `extract_design.dart:348`=fix020 크기 surface 회귀 위험. → **사용자 "건들지 말자" 확정** — 재발해도 처방 안 함(죽은 토큰/효과 측정 게이트 선행 시에만 재검토). 다음 처방은 VW-4·DT-3·FC-1·FC-3로 이동.

## 인과 정직 단서
- ⚠️ N=1 인과 단정 금지 — "묶음2가 죽은 토큰을 *유발*"이 아니라 "묶음2 적용 후 15차 죽은 토큰 관찰(동시발생)·13차 선례·codex 정상작동으로 코퍼스 단독 원인 아님". 자유변수가 *엔진 쪽*일 가능성이 codex 대조로 상당.
- 교훈: **"사소한 흠"으로 분류 시 2층 분리 필수** — 죽은 변수 자체(사소·런타임 무해)와 그것이 드러내는 미구현(안 사소·디자인 충실도)을 구별. 뭉치면 진단 왜곡.
