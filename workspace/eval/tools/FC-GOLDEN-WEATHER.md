# FC 사전등록 산출물 — 골든 오라클 + mutation (날씨 7일 예보 · EVAL-METHOD §2.5)

> **시나리오**: weather (SCENARIO-WEATHER §1 task).
> **상태**: 동결 후보. **명세-독립 외부 행위표** — SCENARIO §1 task + 서버 계약(OpenAPI)만으로 작성, 산출물 코드 무관.
> **코드 미열람 선언**: 이 행위표·mutation은 산출물의 `lib/**`·`test/**`·`design-spec.md`·`scope.md`·`verification-report.md`·`server-contract.json`을 **일절 열람하지 않고** 작성했다. 참조한 것은 (1) SCENARIO-WEATHER.md §1 task 정본, (2) FC-GOLDEN.md(S1) — 형식 골격만, (3) 서버 계약 `openapi-full.json`(외부 진실 — JSON 필드명·enum 값 확인용)뿐이다.
> **작성 시각(freeze stamp)**: 2026-06-14 01:13 (KST, `date '+%Y-%m-%d %H:%M'`).
> **개정(amend) 2026-06-18 12:48 (KST)**: §0 고정 게이트답의 정렬 문구에서 "(서버 순서 유지)" 제거 → "앱 책임·서버 응답 순서 의존 금지"로 정합. 근거 = 프롬프트 `2326dd0`(RUNBOOK §3 verbatim·§4 게이트가 앱측 정렬 명시)·SCENARIO §4와 일치(**A13-1 잔여 해소**). **§1 골든 행위표·§2 mutation·§3 negative-gate 불변** — M1은 이미 "뒤섞은 입력 필수·무정렬=vacuous FAIL"로 정합돼 있어 *측정 대상은 불변*이고 게이트 **문구 모순**만 제거했다. 7차 채점 *착수 전* 창(EVAL-METHOD §0.2·§5)에서 수행 — 소급 아님.
> **작성 인격**: FC 사전등록 전담 인격(조정자·적대 grader·의미 grader와 별개 — EVAL §2.0). **이 산출물의 FC 의미 채점에서는 제외**(작성자=채점자 순환 차단).
> **역할 분리**: 이 *행위표*는 코드 미열람으로 작성. *실행 어댑터*(위젯·provider·route 바인딩)는 산출물마다 달라 **조정자가 코드 열람 후** 작성(§5). "코드 미열람" 선언은 §1~§4에만 적용.

## §0 task·계약 사실 동결 (verbatim 출처만)

**task 행위 목록(SCENARIO-WEATHER §1)**: ① 7일 예보 목록 조회 ② 일별 상세 조회 ③ 목록→상세 내비게이션 ④ 날짜 오름차순 표시 ⑤ 상태 6종 아이콘·색 매핑.

**고정 게이트 답(SCENARIO §4)**: 페이지네이션 안 함(서버도 무페이지네이션 배열) · 로컬 캐시 안 함 · 당겨서 새로고침 **안 함**(디자인엔 visual-only 인디케이터만 — 재조회 행위 골든 **없음**) · 정렬 = **날짜 오름차순(앱 책임 — 서버 응답 순서 의존 금지; 서버 정렬 신뢰=무정렬은 M1에서 vacuous FAIL)** · condition 6종 아이콘·색·한글 라벨 = ui_extension.

**서버 계약(openapi-full.json — 외부 진실)**:
- 목록: `GET /api/v1/weather/` → `200` = **raw JSON 배열** `[WeatherListItem, …]`(페이지네이션 래퍼 없음·무인증). 설명: "오늘부터 7일간의 일별 날씨 예보를 반환한다. 더미 데이터이며 페이지네이션은 적용하지 않는다."
- 상세: `GET /api/v1/weather/{forecast_date}/` → `200` = `WeatherDetail` / `404` = 7일 예보 범위 밖. path param `forecast_date` = 문자열(설명상 `YYYY-MM-DD`·`date.format=date`).
- `WeatherListItem` 필드(전부 required): `date`(string/date) · `condition`(ConditionEnum) · `temp_max`(integer) · `temp_min`(integer).
- `WeatherDetail` 필드(전부 required): 위 4개 + `humidity`(integer) · `wind_speed`(number/double) · `precipitation_prob`(integer).
- `ConditionEnum`(6종·정확히 이 문자열): `clear` · `cloudy` · `overcast` · `rain` · `snow` · `thunderstorm`.

**condition enum ↔ 6종 한글 라벨 대응(계약값↔SCENARIO §1 라벨)**:

| OpenAPI enum (서버 실제값) | SCENARIO §1 한글 라벨 |
|---|---|
| `clear` | 맑음 |
| `cloudy` | 구름많음 |
| `overcast` | 흐림 |
| `rain` | 비 |
| `snow` | 눈 |
| `thunderstorm` | 뇌우 |

> 6종 정확 일대일 — 누락·잉여 없음. `cloudy`=구름많음, `overcast`=흐림 **혼동 주의**(둘 다 흐린 계열이나 별개 enum·별개 라벨·별개 아이콘/색이어야 함).

## §1 골든 행위표 (FC-1) — 외부 행위 사전등록

> 입력 = 서버가 주는 7일 예보(고정). 기대 = 화면에 *보이는 것·동작*. 산출물의 위젯 트리·provider 이름과 무관하게 참인 외부 계약. 각 행에 **관측 방법**(무엇을 보면 PASS/FAIL인지)을 외부 행위로 기술(위젯/provider 이름 언급 금지 — 코드 미열람).

> **고정 입력 데이터셋 D(서버 응답 7건)** — 일부러 **서버 배열 순서를 날짜 비오름차순으로 섞어** 넣는다(정렬 책임이 산출물에 있는지 검증). 날짜는 오늘=`D0`부터 7일 가정.

| 배열 index | date | condition(enum) | temp_max | temp_min |
|---|---|---|---|---|
| [0] | D2 | `rain` | 19 | 12 |
| [1] | D0 | `clear` | 27 | 18 |
| [2] | D5 | `snow` | 1 | -4 |
| [3] | D1 | `cloudy` | 24 | 16 |
| [4] | D6 | `thunderstorm` | 22 | 17 |
| [5] | D3 | `overcast` | 20 | 14 |
| [6] | D4 | `clear` | 26 | 19 |

> (주의: 위 데이터셋의 *배열 순서*는 D2,D0,D5,D1,D6,D3,D4. 올바른 **표시 순서**는 날짜 오름차순 D0→D1→…→D6. 즉 화면에서 첫 항목은 D0/clear/27/18, 마지막은 D6/thunderstorm/22/17 이어야 한다. 만약 산출물이 "서버 순서 유지"를 *배열 순서 그대로*로 오해하면 G-1에서 즉시 드러난다. — 만일 조정자가 라이브 서버가 **이미 날짜 오름차순으로 정렬해 보낸다**고 확인하면, 이 데이터셋을 위젯테스트 fake로 주입해 *정렬키 부재* mutation(M1)을 비-vacuous하게 살릴 수 있다.)

| # | 입력 상태(서버 응답) | 행위 | 기대 결과 · 관측 방법 |
|---|---|---|---|
| **G-1** | 데이터셋 D(7건) | 목록 화면 진입 | **표시 순서 = D0,D1,D2,D3,D4,D5,D6(날짜 오름차순)**. ★핵심: 첫 행 date=D0, 마지막 행 date=D6. **관측**: 목록을 위→아래로 읽어 각 항목의 날짜가 단조 증가(오늘이 맨 위, 가장 먼 날이 맨 아래)인지 본다. 배열 인덱스 순(D2가 맨 위)으로 나오면 FAIL. |
| **G-2** | 데이터셋 D | 목록 화면 진입 | **항목 수 = 정확히 7**. **관측**: 목록 항목 개수를 센다. 7 아니면(6·8·페이지네이션 절단 등) FAIL. |
| **G-3** | 데이터셋 D 중 D0 행(clear/27/18) | 목록 D0 항목 표시 | 한 항목에 **날짜 ∧ 날씨상태 ∧ 최고기온 ∧ 최저기온**이 모두 보임. ★최고/최저 자리 바뀜 없이: D0 항목에서 **최고=27, 최저=18**(27이 high 자리, 18이 low 자리). **관측**: D0 항목을 보고 27이 "최고"로, 18이 "최저"로 표기됐는지 확인. 27↔18 뒤바뀌면(최고 18·최저 27) FAIL. |
| **G-4** | D5 항목(snow/1/-4) | 목록 D5 항목 표시 | **영하·1자리 기온도 올바르게**: 최고=1, 최저=-4(부호·자리 유지). **관측**: 음수 최저기온이 -4로 보이는지(부호 탈락·절댓값화 없이). |
| **G-5** | 데이터셋 D | 목록에서 **D3 항목(overcast) 탭** | **그 날짜(D3)의 상세 화면** 진입 — 탭한 날짜와 상세 날짜 **일치**. **관측**: D3을 탭했는데 상세 화면 상단 날짜가 D3인지(D2·D4 등 이웃 날짜 아님). N번째 탭 → (N±1)번째 상세가 뜨면 FAIL. |
| **G-6** | D3 상세 응답(date=D3·overcast·temp_max=20·temp_min=14·humidity·wind_speed·precipitation_prob) | D3 상세 화면 표시 | **목록 정보(날짜·상태·최고·최저) + 습도 ∧ 풍속 ∧ 강수확률** 3지표 추가 표시. **관측**: 상세 화면에서 습도·풍속·강수확률 3개가 모두 보이는지. 하나라도 누락 시 FAIL. |
| **G-7** | 6종 모두 등장하는 응답(D 데이터셋은 clear·cloudy·overcast·rain·snow·thunderstorm 전부 포함) | 목록 화면 진입(또는 6종 상세) | **6종 상태 각각 서로 구별되는 아이콘 ∧ 색**. ★`cloudy`(구름많음)와 `overcast`(흐림)도 서로 다른 아이콘/색. **관측**: 6개 condition의 아이콘 집합에 중복 없음(6개 distinct) ∧ 색 집합에 중복 없음(6개 distinct) ∧ 6종 중 누락된 상태 없음(미매핑 → 빈/기본 아이콘으로 떨어지면 FAIL). |
| **G-8** | D 데이터셋의 한글 라벨 표기 | 목록/상세에서 상태 텍스트 | enum→한글 라벨 매핑이 §0 대응표대로: clear=맑음·cloudy=구름많음·overcast=흐림·rain=비·snow=눈·thunderstorm=뇌우. **관측**: 각 상태의 한글 표기가 대응표와 일치(영문 enum 노출·라벨 오배치 시 FAIL). |

> **판정 바**: **G-1(날짜 오름차순)·G-5(탭↔상세 날짜 일치)·G-7(6종 구별 매핑)이 핵심**. G-3/G-4(최고·최저 바인딩·부호)·G-6(상세 3지표)이 그 다음. **하나라도 불일치 = FC-1 FAIL(치명).** 정렬을 배열순 그대로 두면 G-1 즉시 FAIL. 최고/최저 바인딩을 바꾸면 G-3·G-4 FAIL. cloudy/overcast를 같은 아이콘·색으로 묶으면 G-7 FAIL.

> **G-7 아이콘 ↔ A1 경계(measure-first 2026-06-18)**: 아이콘 *심볼 선택·distinct*는 RUBRIC A1(시각 충실도·아이콘 처리 비측정·인간 오라클 위임)이라 **자동 채점 비측정·인간 큐(치명 아님)**다. 자동 FC-1/FC-3가 거는 것은 **색 distinct**(6종 색 중복 0)다 — cloudy↔overcast가 동일 *아이콘*이라도 색이 distinct면 화면 6종 구별이 성립해 *인간큐*(라이브런 시각 확인 시 사용자 판정)이지 자동 FAIL 아니고, 동일 *색*이면 자동 FC-1/N4 FAIL이다. (위 'G-7 FAIL'·아래 §3 N4 '같은 아이콘/색' 문언의 *아이콘* 부분은 이 경계로 읽는다 — 7차 claude G-7 거짓 🟡 인간큐 재발 차단.)

## §2 Mutation (FC-2) — 비-vacuous 입증용 (사이트는 조정자가 코드 열람 후)

> 골든 경로상 **핵심 판정마다** 추상 mutation을 사전등록. 주입 사이트(어느 심볼·계층)는 **조정자가 행위표 동결 후 코드 열람해** 식별한다 — 여기서는 *무엇을 깨면 어떤 RED가 나야 하는가*만 적는다. 각 mutation은 **정본 변이 1개**로 고정(택일 OR 금지 — 채점자가 변이를 골라 red가 갈리는 비결정 차단). red = 그 mutation을 주입하면 해당 골든을 두드리는 **행위 검증 테스트(맞는 seam — 판정=순수 도메인 단위 직접 호출·view=VM-override 위젯·통합=integration; EVAL §2.5)**가 깨진다.

| ID | 정본 논리 mutation | 깨는 대상 행위 / 기대 RED 관측 | 주입 사이트(계층·조정자 식별) |
|---|---|---|---|
| **M1** | **날짜 정렬 역전** — 오름차순 비교자를 내림차순으로(또는 정렬키 제거 시 배열순 그대로 노출) | **G-1** 순서 단언 red(첫 항목이 D0가 아니라 D6 또는 D2). 목록 순서 RED. | 정렬이 *코드상 거주한 위치*(domain이면 순수 단위테스트·VM이면 VM-override 위젯테스트 — 복수 거주 시 전부). **뒤섞은 입력 필수**(라이브 서버가 오름차순이라 이미 정렬된 입력은 무정렬 코드도 green·vacuous); 정렬 코드/주입사이트 부재 = 비-vacuity 입증 불가 = FAIL |
| **M2** | **condition→아이콘/색 매핑 교란** — 두 상태 swap(예: `clear`↔`thunderstorm`의 아이콘·색을 맞바꿈) | **G-7**(및 swap된 두 상태의 배지/아이콘) 단언 red — 맑음 자리에 뇌우 아이콘/색이 뜸. 배지 RED. | condition→아이콘/색 매핑(ui_extension 등) |
| **M3** | **최고/최저 기온 바인딩 교환** — 목록(및/또는 상세) 항목에서 temp_max ↔ temp_min 표시 위치 swap | **G-3·G-4** 단언 red — D0 항목 최고가 18·최저가 27로 뜨고, D5 최고가 -4로 뜸. 목록 항목 RED. | 목록 항목 표시 바인딩(item 위젯의 high/low 슬롯) |
| **M4** | **목록→상세 날짜 파라미터 오류** — 탭한 항목의 날짜 대신 인접/고정 날짜를 상세에 전달(예: index N → date[N+1] 또는 항상 첫 날짜) | **G-5** 단언 red — D3을 탭했는데 상세 날짜가 D3이 아님(D2/D4 또는 D0). 내비 RED. | 목록→상세 내비게이션의 날짜 인자 전달부(탭 핸들러·route 파라미터·**날짜→문자열 직렬화 format step(DateTime→path)**) |
| **M5**(선택) | **상세 지표 누락** — 상세에서 습도·풍속·강수확률 중 하나(예: precipitation_prob) 미표시 | **G-6** 단언 red — 상세에 강수확률(또는 택1 지표)이 안 보임. 상세 RED. | 상세 화면 지표 표시부 |

> **주입 사이트(결정적 규칙)**: 각 mutation이 두드리는 골든 경로상 핵심 판정 **각각**. **동작 결정 위치가 여럿이면(정렬을 domain·VM 두 곳에 중복) 전부 주입**(부분 주입 시 다른 경로가 살아 거짓 green). red가 1곳도 안 나면 그 경로가 死코드/중복임을 FC-2 발견 로그에 기록. **단순 freezed 필드·json_serializable codegen은 mutation 대상 제외**(도메인/표시 판정 아님). 러너 = `dart run build_runner build` **선행** 후 `flutter test`(전 스위트 — **정렬 M1·색 distinct M2 = 순수 단위테스트**(도메인 정렬 단위·ui_extension getter 직접 호출·위젯 미펌프) / **기온 슬롯 M3·탭 내비 M4·상세 표시 M5 = VM-override 위젯테스트**; dddart엔 repo provider 없음 — **단 M4 날짜→path 직렬화가 navigator/repo 거주면 VM-override가 구조적 미도달이라 그 직렬화 단위 직접 호출(VO.toApiPath 등)·integration seam으로 두드린다**(`MaterialApp(home:)` VM-override 펌프는 실 GoRouter 미배선이라 navigator 직렬화 round-trip 미경유)). **M1~M4(필수)·M5(선택) 각각 자기 사이트 주입 시 해당 골든 단언 red여야 PASS**(필수 red율 100% 미달 = FC-2 FAIL·vacuous).

> **시나리오 특이사항**: 당겨서 새로고침은 SCENARIO §4 게이트에서 **안 함**(visual-only 인디케이터만) — 재조회 행위 골든·mutation **없음**(S1의 G4·M-새로고침에 해당하는 항목이 weather엔 N/A). 로컬 캐시 없음(네트워크 전용) → 캐시 무효화 골든 없음. 재탭 2단(탭 셸 존재 시 발화)은 *발화 시*만 검증 — 단일 BC 2화면 흐름에서 발화 안 하면 N/A.

## §3 Negative-gate (FC-3) — 명백한 도메인 오류 (하나라도 있으면 FAIL)

> 골든 단언과 별개로, 아래 중 **하나라도 관측되면 즉시 FC-3 FAIL**(개별 골든 통과 여부 무관한 명백한 도메인 깨짐).

- **N1 개수 오류**: 목록 항목이 7개가 아님(서버가 7건을 주는데 6·8·절단·중복 표시).
- **N2 날짜 순서 깨짐**: 목록 날짜가 오름차순이 아님(내림차순·임의·배열순 그대로). 오늘이 맨 위가 아니거나 단조 증가가 깨짐.
- **N3 핵심 필드 누락**: 목록 항목에 날짜·상태·최고기온·최저기온 중 하나라도 빠짐. 또는 상세에 습도·풍속·강수확률 중 하나라도 빠짐.
- **N4 상태 6종 미만/미매핑**: 6종(clear·cloudy·overcast·rain·snow·thunderstorm) 중 일부가 아이콘/색 미매핑(기본·빈 아이콘으로 떨어짐) 또는 두 상태가 같은 아이콘/색 공유(특히 cloudy↔overcast 혼동).
- **N5 기온 바인딩/단위 혼동**: 최고/최저가 뒤바뀜(최저 > 최고로 표시) 또는 음수 최저기온의 부호 탈락(-4 → 4). temp_max/temp_min은 정수(℃ 가정) — 화씨 변환·소수 왜곡 등 단위 혼동.
- **N6 상세 날짜 불일치**: 탭한 날짜와 상세 화면 날짜가 다름(엉뚱한 날짜 상세 진입).
- **N7 enum 라벨 오배치/노출**: 영문 enum(`clear` 등)이 그대로 화면 노출되거나 한글 라벨이 §0 대응표와 어긋남(예: overcast를 "구름많음"으로 표기).

## §4 (종료) 동결 선언

이 §0~§3은 코드 미열람으로 동결한다. §5 실행 어댑터만 조정자가 코드 열람 후 채운다.

## §5 산출물별 실행 어댑터 (조정자 — 코드 열람 후 기록)

> 동일 task라도 산출물(claude판·codex판·plan 분해)마다 위젯·provider·route가 갈림. 행위표(§1)·mutation(§2)·negative-gate(§3)는 공통, 아래만 산출물별. **조정자가 코드 열람 후** 채운다(미열람 선언은 §0~§4에만).

| 산출물 | 목록 화면(위젯·route) | 목록 provider | 정렬 거주 위치 | 상태 매핑(아이콘·색·ui_extension) | 상세 route·날짜 파라미터 | 상세 지표 위젯 |
|---|---|---|---|---|---|---|
| (claude) | | | (열람 후: domain/VM/?) | | | |
| (codex) | | | | | | |

> **어댑터 작성법(seam별 분기)**: ① 행위 종류별 맞는 seam에 테스트 스캐폴드 — **정렬·색 distinct(판정)=순수 단위테스트**(도메인 정렬 단위·ui_extension getter를 *직접 호출*·위젯 미펌프·뒤섞은 입력 D 주입) / **기온 슬롯·탭 내비·상세 표시(view)=VM-override 위젯테스트**(상태보유 VM변종 provider override로 데이터셋 D 주입 — dddart엔 repo/usecase provider 없어 override 불가) / 통합=`integration_test/` → ② §1 G-1~G-8을 그 seam에 맞는 단언(단위=값/컬렉션 단언·위젯=finder 단언)으로 표현 → ③ §2 M1~M5를 식별된 사이트(정렬 비교자·ui_extension 매핑·표시 바인딩·내비 인자) *각각*에 주입해 red 확인(복수 거주 시 전부·주입사이트 死=FAIL). **조정자 메모는 *위치 사실*만**(예: "정렬 비교자 심볼이 파일 X에 존재") — "X가 domain이라 적법" 같은 *적법성 결론* 금지(거주 적법성은 의미 grader 독립 판정·EVAL §2.0). 어댑터·위치 메모는 조정자 보유·grader 미수령.
